# Decision Log — PRD Migração Frontend Discovery AI → React

> Memória canônica e trilha de auditoria. Toda decisão/override registrada aqui.

## 2026-06-05 — Discovery (Fast path)

- **Intent:** Create. Workspace: `_bmad-output/planning-artifacts/prds/prd-discovery-ai-react-migration-2026-06-05/`.
- **Modo de trabalho:** Fast path (usuário forneceu contexto rico — CLAUDE.md, docs brownfield — e tomou decisões-chave explícitas). PRD redigido com `[ASSUMPTION]` onde inferido.
- **Idioma:** PT-BR (preferência confirmada do usuário, sobrepõe `document_output_language: English` do config).

### Decisões do usuário (perguntadas antes de redigir, a pedido dele)
1. **Estratégia de migração:** Big-bang rewrite. Descarta `app.js` vanilla; protótipo de design vira spec visual/comportamental. + "use a versão mais nova de todas as libs".
2. **Linguagem:** TypeScript.
3. **React:** **React 19** (versão mais nova). ⚠️ Override consciente: celebration declara peer dep `react ^18.2.0`. Risco registrado (ver Riscos no PRD); mitigação = validar DS em React 19 cedo, fallback para React 18 se incompatível.
4. **Escopo:** Só conversão de frontend. Backend/segurança/persistência ficam em PRDs separados. Mockar backend com **mirage.js** seguindo `.claude/rules/mirage-rule.md`.

### Outras decisões derivadas
- **Build:** Vite (última versão). [ASSUMPTION] Vite 7.x.
- **i18n:** 3 idiomas — Espanhol, Português, Inglês. [ASSUMPTION] lib `react-i18next` + `i18next` (de facto React); idioma default pt-BR; persistência da escolha em localStorage.
- **Design System:** `@celebration/react` (tema `bees`), tokens `@celebration/assets`, barrel `~/components/external` (convenção Cora Prices em `.claude/rules/components-rule.md`). Storybook de referência: https://celebration.ambevdevs.com.br/storybook-react (adicionado à skill celebration).
- **Roteamento:** `react-router-dom` (exigido pelos componentes de navegação do celebration; substitui hash routing). [ASSUMPTION] manter compat de URLs hash via redirect, a confirmar.
- **Forms:** react-hook-form + zod (padrão do design system).
- **Mock:** mirage.js dev-only (`import.meta.env.DEV`), estrutura flat, espelha contrato dos endpoints atuais do `server.js`.

### Pendências/Overrides a confirmar no Finalize
- ~~React 19 vs peer ^18.2.0~~ **RESOLVIDO** (ver verificação abaixo).
- ~~Disponibilidade do registry~~ **RESOLVIDO** (feed acessível).
- Adotar barrel `~/components/external` + regra components-rule.md neste repo.
- Manter ou descontinuar URLs hash legadas.

## 2026-06-05 — Verificação do feed privado (Azure Artifacts)

- `.npmrc` criado na raiz com scopes `@cora` (cora-frontend) e `@celebration` (design-system, org-level
  AMBEV-SA). **Gitignored** (token não pode vazar). Script `refreshVSToken` adicionado ao package.json.
- `npm view @celebration/react`: latest dist-tag = `2.8.0-alpha.20260603104652`; **stable mais novo = 2.8.1**.
- **Peer deps verificadas (2.8.1):** `react ^19.2.3`, `react-dom ^19.2.3`, `react-router >=6.0.0`.
- **Conclusão:** doc antiga (^18.2.0) obsoleta. **React 19 confirmado como exigência.** Risco R-A eliminado.
- **Novo cuidado (R-B2):** tag `latest` aponta para alpha → fixar `@celebration/react@2.8.1` exato.
- Skill celebration `installation.md` atualizada (peer ^19.2.3, react-router, instruções de registry).

## 2026-06-05 — Respostas a Open Questions

- **Q3 (barrel):** SIM, adotar `~/components/external`. Ação: `components-rule.md` não existe neste repo
  (veio do Cora Prices) → portar/escrever no scaffold. (PRD FR-2 nota.)
- **Q5 (i18n):** confirmado **react-i18next + i18next**. (PRD §4.2 deixou de ser ASSUMPTION.)
- **Q7 (testes):** baseline **dentro do MVP** (unit + component). Adicionado Feature §4.8 + FR-16 + SM-6.
  Stack ASSUMPTION: Vitest + RTL; cobertura inicial 60% statements (a confirmar). E2E amplo fica pós-MVP.
- Ainda abertas: Q4 (URLs hash legadas), Q6 (dono do conteúdo es/en), Q8 (política de versão 2.8.1).

## 2026-06-05 — Finalize

- **Reviewer gate (rubric):** APROVADO com ressalvas. 5/7 dimensões strong, done-ness adequate.
  Review em `review-rubric.md`.
- **Reconciliação de inputs:** `reconcile-sources.md`. Gaps de paridade corrigidos no PRD:
  - Home view → FR-10 (landing + recentes).
  - Chat do Discovery + Modal de entrada de método → FR-14; Glossário atualizado.
  - Entrevistas/transcrição/Modal de gravação → FR-15 apertado.
  - Favoritar Discoveries (`favorite-discovery-ids`) + `research-activity-users` → FR-10.
  - Endpoint `runs/:runId/artifacts` + 7 outputs estruturados + shapes polimórficos
    (artifacts string|obj, status≠current_state, architect/arquiteto) → FR-9.
- **[high] resolvido:** "paridade comportamental" definida operacionalmente em SM-1, ancorada em
  `docs/component-inventory-frontend.md`; R-D atualizado.
- **[medium]:** gate de cobertura só após confirmar meta (report até lá); SM-5 marcado não-bloqueante.
- **[low]:** UJ-3 expandida para template completo (protagonista Carla).
- **Itens deferidos (logados, não bloqueiam):** Q4 (URLs hash), Q6 (dono tradução es/en), Q8 (pin 2.8.1),
  normalização `architect/arquiteto`, escopo de `research-activity-users`, meta de cobertura.
- **Polish:** doc já limpo/PT; pass editorial pesado (skills doc_standards) não executado — stakes internos.
- **Close:** `status: final`, `updated: 2026-06-05`.
